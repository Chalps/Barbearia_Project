import 'package:flutter/material.dart';

class AtendimentoPage extends StatelessWidget {
  const AtendimentoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Atendimento"));
  }
}
